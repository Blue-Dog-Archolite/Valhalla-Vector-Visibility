angular.module('valhalla').controller 'HomeCtrl',['$scope', 'Auth', ($scope, Auth) ->
#      console.log(Auth.currentUser())
      console.log(Auth.isAuthenticated())

      $scope.things = ['Angular', 'Rails 4.1', 'UI Router', 'Together!!']
]
